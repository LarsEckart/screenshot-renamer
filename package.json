{
  "name": "screenshots",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile rename-screenshots.ts --outfile dist/screenshot-renamer && bun build --compile heif-to-png.ts --outfile dist/heif-to-png && bun build --compile image-renamer.ts --outfile dist/image-renamer",
    "install:local": "bun run build && cp dist/screenshot-renamer dist/heif-to-png dist/image-renamer ~/.local/bin/",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix ."
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@nkzw/eslint-plugin": "^2.0.0",
    "@nkzw/oxlint-config": "^1.2.1",
    "@oxlint/plugins": "1.78.0",
    "@types/bun": "^1.3.13",
    "eslint-plugin-no-only-tests": "^3.4.0",
    "eslint-plugin-perfectionist": "^5.9.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-unused-imports": "^4.4.1",
    "oxfmt": "^0.48.0",
    "oxlint": "1.78.0"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
